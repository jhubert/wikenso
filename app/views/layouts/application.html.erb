<!DOCTYPE html>
<html>
<head>
  <title>Hosted, Private Wikis for Teams – Wikenso</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <meta name="viewport" content="width=device-width" />

  <%= render "seo/meta_tags" %>

  <link rel="icon" sizes="16x16 32x32" href="/images/favicon.ico">
  <!--[if IE]><link rel="shortcut icon" href="/images/favicon.ico"><![endif]-->

  <%= render 'vendor/typekit' %>
  <%= render 'vendor/nprogress' %>
  <%= render 'vendor/heap' %>
  <%= render 'vendor/google_analytics' %>

</head>
<body class="<%= yield(:body_class) %>">

<% if subdomain? %>
  <%= render "layouts/header" %>
<% end %>

<% if flash[:notice] %>
  <div class="flash-notice row">
    <p class="large-12-columns">
      <%= flash[:notice] %>
    </p>
  </div>
<% end %>

<% if flash[:error] %>
  <div class="flash-error row">
    <p class="large-12-columns">
      <%= [flash[:error]].flatten.join(",") %>
    </p>
  </div>
<% end %>

<%= yield %>

<%= render "vendor/heap_user_tracking" %>

</body>
</html>
